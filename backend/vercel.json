{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/nodemon"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "server.js"
        }
    ]
}